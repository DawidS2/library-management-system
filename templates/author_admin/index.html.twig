{% extends 'base.html.twig' %}
{% block title %}
    Autor - edycja
{% endblock %}

{% block body %}
    <a class="btn btn-success btn-block" href="{{ path('admin_author_new') }}">+ Dodaj</a>
        <table class="table table-hover table-dark">
            <thead>
            <tr>
                <th>#</th>
                <th>ID</th>
                <th>Imię</th>
                <th>Nazwisko</th>
                <th></th>
            </tr>
            </thead>
        {% for author in authors %}
            <tr>
                <th>{{ loop.index }}</th>
                <td>{{ author.id }}</td>
                <td>{{ author.name }}</td>
                <td>{{ author.surname }}</td>
                <td><a href="{{ path('admin_author_edit', {'id': author.id}) }}">Edytuj</a></td>
            </tr>
        {% endfor %}
    </table>
    <div class="navigation">
        {{ knp_pagination_render(authors) }}
    </div>
{% endblock %}